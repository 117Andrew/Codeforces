#include <iostream>
#include <queue>
using namespace std;
int main()
{
    int n, a, b, c;
    queue <int> q;
    cin >> n;
    for (int i=0;i<n;i++)
    {
        cin >> a >> b >>c;
        q.push(a);
        q.push(b);
        q.push(c);
    }
    for (int i=0;i<n;i++)
    {
        
    }    
    return 0;
}